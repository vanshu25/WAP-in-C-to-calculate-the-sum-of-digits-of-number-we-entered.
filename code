#include<stdio.h>
void main()
{
  int sum=0, n, i;
  printf("enter the number:\n");
  scanf("%d",&n);
  while(n!=0)
  {
    i = n%10;
    sum = sum+i;
    n = n/10;
  }
  printf("sum of digits of entered number: %d",sum);
}
